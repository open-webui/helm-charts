apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcpo.fullname" . }}-test-connection"
  labels:
    {{- include "mcpo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcpo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
