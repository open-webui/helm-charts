apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcpo.fullname" . }}-config
  labels:
    {{- include "mcpo.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.config | toJson | nindent 4 }}