apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcpo.fullname" . }}
  labels:
    {{- include "mcpo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcpo.selectorLabels" . | nindent 4 }}
